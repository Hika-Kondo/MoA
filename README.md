# setup

```
mkdir kaggle/input
unzip lish-moa.zip -d kaggle/input
```

# set up ```.env```

```
sh setDotEnv.sh
```

# set up docker-compose

```
docker-compose build
```

# up docker-compose

```
docker-compose up
```
